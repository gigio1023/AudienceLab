# OpenAI
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-5-mini
OPENAI_REASONING_EFFORT=low
OPENAI_COMPUTER_USE_MODEL=computer-use-preview
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_AUTO_ACK_SAFETY_CHECKS=false
AGENT_LOG_LEVEL=INFO

# Local SNS (SNS-Vibe)
SNS_URL=http://localhost:51737
SNS_EMAIL=agent1@local.dev
SNS_PASSWORD=password
# SNS_USERNAME=agent1

# MCP (auto-started when running `uv run python local_agent.py`)
MCP_REQUIRE_APPROVAL=never
MCP_MAX_STEPS=35
MCP_STEP_DELAY_MIN=1.0
MCP_STEP_DELAY_MAX=3.0
