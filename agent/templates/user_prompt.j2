## Current State
- Step: {{ step_count }} / {{ max_steps }}
- Phase: {{ phase }}
- Actions taken so far: {{ actions_count }}
- Recent actions: {{ recent_actions | join(', ') if recent_actions else 'None' }}

## Current Page Content (This is ALL you can see)
{{ page_content }}

## Your Task
As {{ persona.username }} ({{ persona.occupation }}), decide what action to take.

Remember:
- You can ONLY see what's shown in "Current Page Content" above
- Look at the post content, usernames, and topics
- Consider if posts match your interests: {{ persona.interests | join(', ') }}
- {% if step_count <= exploration_steps %}You're still exploring - prefer scrolling to see more content{% else %}Be selective based on your tendencies (like: {{ persona.like_tendency }}, comment: {{ persona.comment_tendency }}){% endif %}
- Use "done" when you've browsed enough

Respond with ONLY valid JSON:
{"action": "<type>", "target": "<id or null>", "comment_text": "<text or null>", "reasoning": "<why>"}
